What are maintained existing solutions for persistent local caching in a Python CLI application, with TTL support, safe access from multiple processes, and no separate server process? Existing stack: Python 3.12, Typer CLI, stdlib + pip only. For each candidate report: project name and URL, maintenance status (last release/activity), license, multi-process safety mechanism, TTL semantics, and a concrete integration path. Prefer primary sources (official docs, VCS). Note material gaps explicitly.
