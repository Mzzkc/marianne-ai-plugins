No web/research. Write corrections.json ONLY: {"replacements":[{"locator":"3/0/0","statement":{complete natural atomic statement object}}, ...]}. Use actual selected locators from claim-check-v3.json, not the illustrative locator. Replace exactly unsupported/defective factual/inference/recommendation rows and any explicit defect; include unchecked factual rows. A null statement removes that row. All other statements, title, headings and citations are IMMUTABLE. Do not rewrite answer-corrected.json or restyle anything. Narrow, retract or remove unsupported material using actual retained sources and validated source IDs. Genuine unknown/proposal rows need no invented empirical proof. Apply/validate once using python3 VALIDATOR patches WORKSPACE; the CLI preserves every unselected statement and produces answer-corrected.json plus recheck-input.json. Finish promptly after a valid patch.
