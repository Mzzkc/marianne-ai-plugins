No new network. Any earlier typed-quote claim check is a rejected artifact, not proof; do not copy its support verdicts. Judge each current answer claim against actual passages afresh. Read answer-draft.json and actual cited bodies. Write claim-check-v3.json. Check factual correctness AND relevance to the original question; source presence is not entailment. Use passages-index.json to read actual deterministic excerpts and original bodies as needed. For each answer statement record locator as zero-based section/paragraph/statement indices joined by / (for example 0/1/0), exact claim text, verdict supported/defect/unverified, specific reason and evidence [{source_id,passage_ids:["P0001"]}] selected from that claim's own cited source in passages-index.json. Read its passage_file and select actual stable IDs; NEVER type or paraphrase quotations. The helper reconstructs their exact text from retained bodies. A passage must support the actual scope; ID presence is merely a provenance check, not entailment. Licensing, versions, maintenance and tested compatibility require direct evidence; no legal conclusions from license names or sockets. Mark missing/error/metadata-only support unverified or defect, never supported. Unsupported factual/inference/recommendation statements trigger the correction step; genuine labeled proposals/unknowns need no invented empirical proof. Include all statements if affordable; coverage_complete=false if not fully assessed, never invent exact coverage. Output {answer_sha256:"actual hash",coverage_complete:boolean,checks:[...]}. A material error is defect; an unverified claim is explicit uncertainty, not verified. Do not change the answer. Record findings incrementally; no new judge or research. Before completing, validate with python3 VALIDATOR review WORKSPACE. If the mechanical ID/body join fails, select the actual existing passage IDs; never invent excerpts.

Also inspect consequential omissions in an affected recommendation: if retained cited documentation imposes a material reuse restriction, dropping it can mislead even when each explicit sentence is literally true. Mark the affected locator defect with the exact source-scoped passages, so the single bounded patch can add or narrow the caveat. Do not invent restrictions or infer output rights from weight/model metadata.
