# Implementation disposition

Canonical source: `/home/emzi/Projects/SCORES/marianne-search-20260911/SOURCES-QUALITY`.
Portable distribution: the authorized plugin-quality candidate's
`marianne/skills/research`. No runtime engine or profile changes are required.

A has prepare → strategy → assignment gate → configured native parallel seats →
synthesis → delivery. B inserts one fresh bounded challenge before synthesis.
The default graphs have 7/8 total movements and 4/5 AI calls. Lab has preparation,
two independent reviewers and delivery; caller owns synthesis. Every AI node
requires the original directory, prompt.md and current receipt. Native movement
aliases, fixed dependencies and cardinality are generated from one roster.

Migration: the unreleased collector score variants and tiny examples are now
`tests/fixtures/legacy-search`, outside public skill discovery. Its exact accepted
helper is retained as `research/scripts/snapshot.py`; all 47 old tests remain.
Only test imports/fixture paths changed. New graph/role/context tests replace
public routing expectations. Thin marianne-search and thinking-lab entries point
to research. Symlinked legacy lab score paths resolve to one score source. Local
separately operated scores and historical runs are untouched. Instrument catalog
usage references were repaired; historical changelog narratives were not rewritten.

Validation is deliberately split: coverage, schema, source/candidate joins,
run identity and context delivery are deterministic; relevance, truthful web
claims, actual model identity and research superiority need live independent
judgment. Native fixture assembly is labeled provider-free, never a release lock.
The release checker reports missing future run artifacts and undefined score_dir;
its limitations were reproduced without manufacturing inputs or changing it.

Root must freeze/probe exact model routes and equal budgets, run A/B with identical
original bytes in separate fresh workspaces/provider sessions, verify every actual
attempt receipt, evaluate source truth independently, and decide acceptance and
publication. The global A/B wall-clock ceilings are commissioning obligations,
not an implemented score-wide timeout. On native stage failure, initial partial
status and useful artifacts survive; explicit `deliver` packages partial evidence
with nonzero exit, so it cannot success-chain.

Bounded delivery correction: human reports now render validated requirement and
candidate identity legends and include hashed strategy.json for search. The default
two-seat cast is `claude-sonnet-4-6` plus `gemini-3.8-flash-high` through
Antigravity; Gemini Flash also owns strategy/challenge, while explicit
`codex-cli` `gpt-5.6-terra` owns synthesis. The Gemini high tier is part of its
model ID, not an arbitrary config effort flag. Its prior search-and-fetch evidence
does not qualify Sonnet: Sonnet fetched a cached primary page but its native
search returned GenerateContent with no summary and exposed no model metadata.
Keep it UNQUALIFIED until conductor qualification proves both functions. Choose
economical qualified search models and increase synthesis size only for a concrete
failed obligation. Monetary cost is unknown; paid entitlement is not free. The
concert wrapper example has a fixed Codex consumer outside the research roster.
