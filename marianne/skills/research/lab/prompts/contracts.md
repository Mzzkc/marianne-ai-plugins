# Independent supplied-context review

Use the current run receipt and original context. Write review-N.md addressing the caller’s actual question, concrete findings, uncertainties and tradeoffs. No mandatory web search, application changes or job launch. Caller owns synthesis. Write review-N.json {"schema_version":1,"kind":"research-review","run_id":"current receipt ID","review":"review-N","sha256":"actual SHA256 of exact markdown bytes"}. Compute hashes with a local tool; never invent them. A shape/hash pass proves artifact custody, not truth.
