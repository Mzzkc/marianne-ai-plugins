{"id": "C001", "frame_agrees": "partial", "reasoning": "Claim correctly identifies single-brace format syntax but omits the available context variables (workspace, sheet_num, start_item, end_item, sheet_id, movement, voice, voice_count) and KeyError-on-missing-key behavior. An expert writing validation path templates needs to know which placeholders are valid."}
{"id": "C002", "frame_agrees": "partial", "reasoning": "Correct on deferred rendering and Jinja2 syntax, but omits which component performs the rendering (PromptRenderer.render in the baton musician via AttemptContext). An expert debugging prompt rendering failures needs to trace to the specific renderer, not just 'the baton.'"}
{"id": "C003", "frame_agrees": "true", "reasoning": "Sufficiently complete: identifies raw Jinja2 storage, dispatch-time rendering, and the responsible component (baton). An expert has enough to locate the rendering logic."}
{"id": "C004", "frame_agrees": "true", "reasoning": "Claim captures the key architectural fact (free function vs method) and its stateless nature. No critical expert-facing omission."}
{"id": "C005", "frame_agrees": "partial", "reasoning": "Omits the default ceiling value (10 per docs/daemon-guide.md) and the hard-stop return semantics (dispatch halts entirely, not a per-sheet skip). An expert tuning concurrency needs both the default and the behavior when the ceiling is hit."}
{"id": "C006", "frame_agrees": "partial", "reasoning": "Omits the fallback chain: when a model-specific limit is absent, the scheduler falls back to instrument_concurrency.get(instrument). An expert configuring limits needs to know this priority/fallback behavior to avoid unintended permissive defaults."}
{"id": "C007", "frame_agrees": "partial", "reasoning": "Omits critical behavioral semantics: rate-limited instruments are skipped (continue to next sheet), not hard-stopped. Also omits the source of the set (baton.get_rate_limited_instruments()). An expert needs skip-vs-stop semantics to predict dispatch behavior under rate limiting."}
{"id": "C008", "frame_agrees": "partial", "reasoning": "Omits the configurable range (0-5000ms per score-writing-guide), that different instruments are NOT staggered against each other (only same-instrument bursts), and that staggered sheets schedule a DispatchRetry wake event to re-attempt later. Expert needs all three to reason about stagger behavior."}
{"id": "C009", "frame_agrees": "true", "reasoning": "Narrow claim fully captured: asyncio.Queue with no maxsize is unbounded by definition. No expert-facing omission for this specific assertion."}
{"id": "C010", "frame_agrees": "true", "reasoning": "Claim lists all three kinds and references the ECS pattern. Sufficient for an expert to understand the technique taxonomy."}
{"id": "C011", "frame_agrees": "true", "reasoning": "Simple, complete claim: TechniqueConfig has a required kind field. No omission."}
{"id": "C012", "frame_agrees": "partial", "reasoning": "Omits the specific functions performing resolution (filter_techniques_for_phase, resolve_techniques_for_sheet) and what 'phases' concretely means (sheet dispatch phases). An expert extending the technique system needs entry-point function names."}
{"id": "C013", "frame_agrees": "false", "reasoning": "Critically omits the routing priority order (A2A -> tool calls -> code blocks -> prose) which is the core behavioral contract of TechniqueRouter. Also uses imprecise enum name 'A2A' instead of 'A2A_REQUEST'. An expert cannot understand routing behavior without the priority order — classification alone is half the story."}
{"id": "C014", "frame_agrees": "partial", "reasoning": "Omits the concrete token budget target (<500 tokens per test assertions) and the stub format (def name(...) -> T: ... with type hints + ellipsis). An expert evaluating prompt budget impact needs the quantitative target, not just 'compact.'"}
{"id": "C015", "frame_agrees": "true", "reasoning": "Claim lists all 9 types explicitly. Complete for an expert needing to know which validations are retryable."}
{"id": "C016", "frame_agrees": "partial", "reasoning": "Omits that start_new_session=True is hardcoded (not configurable) for validation commands, unlike cli_backend.py where it is profile-driven. An expert needs this distinction to understand why behavior differs between validation and instrument command paths."}
{"id": "C017", "frame_agrees": "false", "reasoning": "Critically omits that the RuntimeError raised by the guard is caught by the outer try/except at engine.py:1163 and silently converted to a failed ValidationResult — the refusal does NOT propagate as an exception. Also no test covers this for the engine path (only cli_backend.py is tested). An expert would wrongly assume the RuntimeError surfaces; instead it is masked into an ordinary validation failure."}
{"id": "C018", "frame_agrees": "true", "reasoning": "Claim captures the full kill sequence: SIGTERM, 2-second grace, SIGKILL. Complete for an expert reasoning about process cleanup."}
{"id": "C019", "frame_agrees": "partial", "reasoning": "Omits that tests still reference the deleted module via mock.patch (test_daemon_e2e.py:971,1190), meaning the stale docstring has downstream test consequences. Also citation is imprecise: stale docstring is at lines 10-16, not 20-25 (which is __all__). Expert needs to know test impact."}
{"id": "C020", "frame_agrees": "partial", "reasoning": "Omits the specific SDK features justifying the Doctrine Exception: thinking, streaming, and tool_use capabilities that a generic HTTP POST cannot replicate. An expert evaluating whether to add more native backends needs to know the justification criteria, not just that an exception exists."}
{"id": "C021", "frame_agrees": "false", "reasoning": "Critically omits how recursive_light actually works at runtime: it is dispatched via the generic OpenAI-compat handler (OpenRouterBackend in backend_pool.py:227-258), not simply 'missing a backend.' The claim implies non-functionality, but the instrument IS operational through a generic path. An expert would incorrectly conclude recursive_light is broken."}
{"id": "C022", "frame_agrees": "partial", "reasoning": "Omits the CVE justification (CVE-2026-34450, CVE-2026-34452) and version constraint (>=0.87.0) that explain WHY it is a hard dependency. An expert deciding whether to make it optional needs the security rationale."}
{"id": "C023", "frame_agrees": "false", "reasoning": "Critically ambiguous and incomplete: omits which specific executors remain (only AnthropicApiBackend and OllamaBackend), that generic HTTP executors were deliberately retired and relocated to execution/instruments, and that this is documented as architectural drift. An expert cannot determine the package's actual contents from this claim."}
{"id": "C024", "frame_agrees": "partial", "reasoning": "Omits the default model (claude-sonnet-4-5-20250929) and that it uses the Messages API specifically. An expert configuring models needs the default to avoid unexpected billing."}
{"id": "C025", "frame_agrees": "partial", "reasoning": "Omits the MCP-to-Ollama tool translation capability (a core differentiating feature) and that there is no official Ollama SDK (native = first-party httpx implementation). An expert evaluating backend capabilities needs the tool translation detail."}
{"id": "C026", "frame_agrees": "true", "reasoning": "Claim captures the key fact (in-memory, not checkpointed) with sufficient specificity (_a2a_inboxes). No critical omission for an expert assessing restart-safety."}
{"id": "C027", "frame_agrees": "partial", "reasoning": "Omits that the event IS serialized via to_observer_event (events.py:902) for observability/dashboard purposes — it is not entirely dead code. An expert pruning unused code needs to know the observer path is active before removing the class."}
{"id": "C028", "frame_agrees": "partial", "reasoning": "Same omission as C027: A2ATaskFailed is serialized via to_observer_event (events.py:914) for observability. Not entirely dead — observer serialization is wired even though execution handling is not."}
{"id": "C029", "frame_agrees": "false", "reasoning": "Critically omits the documentation/implementation contradiction: design docs (baton-design.md) describe CronTick as fully functional ('submits the score as a new job and schedules the next tick'), but the implementation is a warning-only stub. An expert reading docs would believe the feature works; the claim should flag this dangerous gap."}
{"id": "C030", "frame_agrees": "false", "reasoning": "Critically omits the documentation/implementation contradiction: design docs describe ConfigReloaded as handled ('rebuilds pending sheets'), but the implementation is a warning-only stub. An expert relying on docs would expect live config reload; the claim should flag this gap."}
{"id": "C031", "frame_agrees": "partial", "reasoning": "Omits that config validation still actively runs (raises ValueError if grounding is enabled without hooks configured) and that GroundingDecisionContext scaffolding exists in summary.py. An expert might assume the feature is entirely absent rather than config-wired-but-runtime-unwired."}
{"id": "C032", "frame_agrees": "true", "reasoning": "Claim captures the disabling, the no-op behavior, and the WSL2 root cause. Sufficient for an expert to understand why reaping is off."}
{"id": "C033", "frame_agrees": "false", "reasoning": "Claim is factually false and critically misleading: no module-level disabled flag exists. Disabling is via inline no-op returns in method bodies (reap_orphaned_backends returns [] at :281, _reap_orphans_proc returns [] at :320). An expert would search for a flag to toggle reaping back on — none exists."}
{"id": "C034", "frame_agrees": "true", "reasoning": "Substance is complete: fcntl.flock with LOCK_EX|LOCK_NB advisory locking on PID file. Citation imprecision (lines 85-90 vs actual 1194-1199) does not affect expert understanding."}
{"id": "C035", "frame_agrees": "partial", "reasoning": "Omits that typer.Exit() is raised after printing (terminating the CLI) and that the callback is wired to --version/-V flags. An expert testing CLI behavior needs to know the exit semantics."}
{"id": "C036", "frame_agrees": "true", "reasoning": "Claim captures both the $0 fallback and the cost_uncertain flag. Complete for an expert reasoning about cost reporting fidelity."}
{"id": "C037", "frame_agrees": "partial", "reasoning": "Omits a critical distinction: DaemonConfig.state_backend_type is separately frozen to sqlite (not configurable), while JobConfig.state_backend is the one described here. An expert might confuse the two config levels and attempt to set a JSON daemon backend."}
{"id": "C038", "frame_agrees": "partial", "reasoning": "Omits that absolute workspace paths are preserved unchanged (only relative paths are resolved). An expert using absolute paths needs to know they bypass resolution."}
{"id": "C039", "frame_agrees": "partial", "reasoning": "Omits that the validator also emits a UserWarning when NEITHER template nor template_file is specified (not just ValueError when both are set). An expert needs the full validation contract: both-set = error, neither-set = warning."}
{"id": "C040", "frame_agrees": "true", "reasoning": "Claim captures the backward-compat aliases and their purpose. Sufficient for an expert maintaining legacy score compatibility."}
{"id": "C041", "frame_agrees": "true", "reasoning": "Claim lists all 7 states explicitly. Complete for an expert reasoning about job lifecycle."}
{"id": "C042", "frame_agrees": "true", "reasoning": "Claim lists all 11 states with the scheduling range. Complete for an expert tracing sheet execution."}
{"id": "C043", "frame_agrees": "true", "reasoning": "Claim lists all 6 categories. Complete for an expert analyzing execution outcomes."}
{"id": "C044", "frame_agrees": "true", "reasoning": "Value (4) is correct and sufficient. An expert performing migrations needs the current version number, which is provided."}
{"id": "C045", "frame_agrees": "true", "reasoning": "Value (15) is correct and sufficient. An expert performing migrations needs the current version number, which is provided."}
{"id": "C046", "frame_agrees": "true", "reasoning": "Claim captures the IntEnum type and the inverse-severity semantics. Complete for an expert using severity comparisons."}
{"id": "C047", "frame_agrees": "false", "reasoning": "Claim is factually false: categories are parsed from the FIRST numeric digit (self.value[1], i.e., the character immediately after 'E' prefix), not the 'second digit.' Source docstring explicitly says 'first digit.' An expert would implement incorrect parsing logic based on this claim."}
{"id": "C048", "frame_agrees": "partial", "reasoning": "Omits that MAX_OUTPUT_CAPTURE_BYTES captures the LAST N bytes (trailing) of stdout/stderr, not the first N. An expert diagnosing truncated output needs to know which end is preserved. Also, stale test comments reference '10KB' causing confusion."}
