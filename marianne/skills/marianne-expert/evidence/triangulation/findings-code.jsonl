{"id": "C001", "verdict": "SUPPORTS", "evidence_path": "src/marianne/execution/validation/engine.py", "evidence_excerpt": "expanded = path_template.format(**context)", "notes": "The method uses Python single-brace `.format(**context)` to expand path_template."}
{"id": "C002", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/sheet.py", "evidence_excerpt": "Prompt rendering is deferred. The template stays unrendered because\ncross-sheet context ({{ previous_outputs[2] }}) only exists after earlier\nsheets complete. The baton renders at dispatch time.", "notes": "The docstring supports this claim explicitly."}
{"id": "C003", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/sheet.py", "evidence_excerpt": "    prompt_template: str | None = Field(\n        default=None,\n        description=\"Raw Jinja2 template (rendered at dispatch time by the baton)\",\n    )", "notes": "The `prompt_template` property is defined as a field with a description stating it is rendered at dispatch time by the baton."}
{"id": "C004", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/dispatch.py", "evidence_excerpt": "- dispatch_ready() is a free function, not a method on BatonCore.\n...\nasync def dispatch_ready(\n    baton: BatonCore,\n    config: DispatchConfig,\n    callback: DispatchCallback,\n) -> DispatchResult:", "notes": "The module docstring explicitly states dispatch_ready() is a free function, and the function is defined globally."}
{"id": "C005", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/dispatch.py", "evidence_excerpt": "            # Check global concurrency\n            if global_running >= config.max_concurrent_sheets:", "notes": "The dispatch loop checks if the number of running sheets meets or exceeds `max_concurrent_sheets` and stops scheduling if so."}
{"id": "C006", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/dispatch.py", "evidence_excerpt": "            instrument = sheet.instrument_name or \"\"\n            model_key = f\"{instrument}:{sheet.model}\" if sheet.model else instrument\n            model_limit = config.model_concurrency.get(model_key)\n            if model_limit is None:\n                model_limit = config.instrument_concurrency.get(instrument)", "notes": "The scheduler retrieves limits based on `instrument:model` keys before falling back to instrument concurrency."}
{"id": "C007", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/dispatch.py", "evidence_excerpt": "            # Check instrument rate limit (transient — don't fallback)\n            if instrument in config.rate_limited_instruments:", "notes": "The scheduler skips dispatching sheets if their instrument is in the `rate_limited_instruments` set."}
{"id": "C008", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/dispatch.py", "evidence_excerpt": "            if config.stagger_delay_ms > 0:\n                inst_state = baton.get_instrument_state(instrument)\n                if (\n                    inst_state is not None\n                    and now - inst_state.last_dispatch_at\n                    < config.stagger_delay_ms / 1000.0\n                ):", "notes": "Uses `time.monotonic` as the default source for `now` and `last_dispatch_at` comparison to enforce a stagger window."}
{"id": "C009", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/core.py", "evidence_excerpt": "        self._inbox: asyncio.Queue[BatonEvent] = inbox or asyncio.Queue()", "notes": "The asyncio.Queue is instantiated without a `maxsize` argument, making it unbounded by default."}
{"id": "C010", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/config/techniques.py", "evidence_excerpt": "class TechniqueKind(str, Enum):\n...\n    SKILL = \"skill\"\n    MCP = \"mcp\"\n    PROTOCOL = \"protocol\"", "notes": "The enum defines the three kinds: skill, mcp, and protocol."}
{"id": "C011", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/config/techniques.py", "evidence_excerpt": "    kind: TechniqueKind = Field(\n        description=\"Technique kind: skill, mcp, or protocol\",\n    )", "notes": "TechniqueConfig contains a required `kind` field of type `TechniqueKind`."}
{"id": "C012", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/techniques.py", "evidence_excerpt": "Resolves which techniques are active for a given sheet phase, generates\ntechnique manifests, and discovers technique skill documents for prompt\ninjection.", "notes": "Technique resolution filters and resolves active techniques per phase for sheet prompt assembly."}
{"id": "C013", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/technique_router.py", "evidence_excerpt": "class OutputKind(str, Enum):\n    \"\"\"Classification of agent output for routing decisions.\"\"\"\n\n    PROSE = \"prose\"\n    CODE_BLOCK = \"code_block\"\n    TOOL_CALL = \"tool_call\"\n    A2A_REQUEST = \"a2a_request\"", "notes": "OutputKind classifies output formats into PROSE, CODE_BLOCK, TOOL_CALL, and A2A_REQUEST."}
{"id": "C014", "verdict": "SUPPORTS", "evidence_path": "src/marianne/execution/interface_gen.py", "evidence_excerpt": "    def generate_stubs(self, declarations: Sequence[TechniqueDeclaration]) -> str:\n        \"\"\"Generate compact prompt-injected Python stubs.\"\"\"", "notes": "InterfaceGenerator generates compact stubs for MCP tools to reduce prompt size."}
{"id": "C015", "verdict": "SUPPORTS", "evidence_path": "src/marianne/execution/validation/engine.py", "evidence_excerpt": "    _RETRYABLE_VALIDATION_TYPES = frozenset({\n        \"file_exists\",\n        \"file_modified\",\n        \"content_contains\",\n        \"content_regex\",\n        \"command_succeeds\",\n        \"path_in_scope\",\n        \"field_match\",\n        \"file_sha256\",\n        \"csv_unique_key\",\n    })", "notes": "The frozenset contains exactly these 9 validation types."}
{"id": "C016", "verdict": "SUPPORTS", "evidence_path": "src/marianne/execution/validation/engine.py", "evidence_excerpt": "        # Process Lifecycle Phase 1: spawn with start_new_session=True so\n...\n            proc = await asyncio.create_subprocess_exec(\n                \"bash\", \"-c\", expanded_command,\n...\n                start_new_session=True,", "notes": "Commands are spawned using `start_new_session=True` so child processes are grouped together under a single pgid."}
{"id": "C017", "verdict": "SUPPORTS", "evidence_path": "src/marianne/execution/validation/engine.py", "evidence_excerpt": "                if daemon_pgid is not None and pgid == daemon_pgid:\n...\n                    raise RuntimeError(\n                        f\"Validation command shares daemon pgid ({pgid}); \"\n                        \"refusing to continue\"\n                    )", "notes": "Aborts with a `RuntimeError` if the spawned command's PGID matches the daemon's PGID."}
{"id": "C018", "verdict": "SUPPORTS", "evidence_path": "src/marianne/execution/validation/engine.py", "evidence_excerpt": "                if proc is not None and proc.returncode is None:\n                    if pgid is not None:\n                        try:\n                            _safe_killpg(pgid, signal.SIGTERM, context=\"validation.kill_grace\")\n                        except (ProcessLookupError, PermissionError):\n                            pass\n                        try:\n                            await asyncio.wait_for(proc.wait(), timeout=2.0)\n                        except TimeoutError:\n                            try:\n                                _safe_killpg(pgid, signal.SIGKILL, context=\"validation.kill_force\")", "notes": "The cleanup block sends SIGTERM, waits up to 2 seconds, and escalates to SIGKILL if the process hasn't exited."}
{"id": "C019", "verdict": "SUPPORTS", "evidence_path": "src/marianne/backends/__init__.py", "evidence_excerpt": "The legacy ``ClaudeCliBackend`` class has been relocated out of this\npackage to\n``marianne.execution.instruments.claude_cli_legacy.ClaudeCliBackend``", "notes": "The docstring references `claude_cli_legacy`, which has been completely deleted from `src/marianne/execution/instruments/`."}
{"id": "C020", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/backend_pool.py", "evidence_excerpt": "def _build_anthropic_family_backend(\n    profile: InstrumentProfile,\n    *,\n    working_directory: Path | None,\n    model: str | None,\n    api_key: str | None,\n) -> Backend:\n    \"\"\"Construct a Backend for schema_family='anthropic' profiles.\n\n    Uses the native AnthropicApiBackend (Doctrine Exception Registry: the\n    Anthropic SDK provides thinking/streaming/tool_use features that a\n    generic HTTP POST cannot replicate, so this backend remains native).\n    \"\"\"\n    from marianne.backends.anthropic_api import AnthropicApiBackend", "notes": "Instantiation of `AnthropicApiBackend` is hardcoded for `anthropic` family profiles because of Doctrine Exceptions."}
{"id": "C021", "verdict": "SUPPORTS", "evidence_path": "src/marianne/backends/", "evidence_excerpt": "The directory contains only __init__.py, anthropic_api.py, base.py, and ollama.py. No Python backend module exists for recursive_light.", "notes": "The `recursive_light` profile is defined in `src/marianne/instruments/registry.py`, but there is no `recursive_light.py` backend module in `src/marianne/backends/`."}
{"id": "C022", "verdict": "SUPPORTS", "evidence_path": "pyproject.toml", "evidence_excerpt": "    \"anthropic>=0.87.0\",         # CVE-2026-34450, CVE-2026-34452", "notes": "The `anthropic` library is listed in the main `dependencies` array rather than under optional features or dev dependencies."}
{"id": "C023", "verdict": "CONTRADICTS", "evidence_path": "src/marianne/backends/__init__.py", "evidence_excerpt": "Only the two documented exceptions (``AnthropicApiBackend`` and\n``OllamaBackend``) remain as native Python backend modules under\n``src/marianne/backends/``.", "notes": "The backends package does not contain native HTTP executors; generic/native HTTP executors have been retired, leaving only AnthropicApiBackend and OllamaBackend. OpenAICompatBackend and PluginCliBackend reside in the execution/instruments package."}
{"id": "C024", "verdict": "SUPPORTS", "evidence_path": "src/marianne/backends/anthropic_api.py", "evidence_excerpt": "class AnthropicApiBackend(Backend):\n    \"\"\"Run prompts directly via the Anthropic API.\n\n    Uses the official anthropic SDK for direct API access.", "notes": "AnthropicApiBackend implements direct API access using the official `anthropic` SDK."}
{"id": "C025", "verdict": "SUPPORTS", "evidence_path": "src/marianne/backends/ollama.py", "evidence_excerpt": "class OllamaBackend(HttpxClientMixin, Backend):\n    \"\"\"Backend for Ollama model execution with tool translation.\n\n    Implements the Backend protocol for local Ollama models.", "notes": "Implements local model execution and handles communication with Ollama's API endpoints using `httpx`."}
{"id": "C026", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/adapter.py", "evidence_excerpt": "        # Internal A2A runtime state. Agent cards are registered per running\n        # job; inboxes hold delegated tasks until the target's next A2A-enabled\n        # sheet renders its prompt. These structures are in-memory only today;\n        # restart persistence requires a checkpoint schema extension.\n        self._a2a_registry = AgentCardRegistry()\n        self._a2a_inboxes: dict[str, A2AInbox] = {}", "notes": "The docstring explicitly states that A2A states are in-memory only and requires a checkpoint schema extension for restart persistence."}
{"id": "C027", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/events.py", "evidence_excerpt": "class A2ATaskCompleted:", "notes": "`A2ATaskCompleted` is defined, annotated, and serialized, but is never instantiated, fired, or handled in the core baton logic."}
{"id": "C028", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/events.py", "evidence_excerpt": "class A2ATaskFailed:", "notes": "`A2ATaskFailed` is defined, annotated, and serialized, but is never instantiated, fired, or handled in the core baton logic."}
{"id": "C029", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/core.py", "evidence_excerpt": "                case CronTick():\n                    _logger.warning(\n                        \"baton.event.unimplemented\",\n                        extra={\"event_type\": \"CronTick\"},\n                    )", "notes": "The case statement handles `CronTick` by logging it as unimplemented."}
{"id": "C030", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/core.py", "evidence_excerpt": "                case ConfigReloaded():\n                    _logger.warning(\n                        \"baton.event.unimplemented\",\n                        extra={\"event_type\": \"ConfigReloaded\"},\n                    )", "notes": "The case statement handles `ConfigReloaded` by logging it as unimplemented."}
{"id": "C031", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/config/job.py", "evidence_excerpt": "    grounding: GroundingConfig = Field(default_factory=GroundingConfig)", "notes": "GroundingConfig is imported and configured, but there is no runtime path in the orchestrator/runner that executes or integrates grounding hooks."}
{"id": "C032", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/pgroup.py", "evidence_excerpt": "        \"\"\"System-wide scan for orphaned backend child processes.\n\n        .. warning:: DISABLED — This method is a no-op.\n...\n           On WSL2, killing \\`\\`systemd\n           --user\\`\\` cascades into \\`\\`systemd-poweroff.service\\`\\` and shuts\n           down the entire VM (observed 9 times, exit code 9, all\n           terminals dead).", "notes": "The method is documented as disabled and a no-op, specifically noting the VM shutdown issue on WSL2."}
{"id": "C033", "verdict": "CONTRADICTS", "evidence_path": "src/marianne/daemon/pgroup.py", "evidence_excerpt": "Process group manager has a module-level disabled flag for reaping orphans.", "notes": "reap_orphaned_backends is disabled by being hardcoded as a no-op in its implementation, and there is no module-level flag/variable controlling it."}
{"id": "C034", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/process.py", "evidence_excerpt": "        fd = os.open(str(pid_file), os.O_RDONLY)\n        fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)", "notes": "Atomically writes the PID file and uses `fcntl.flock` to apply an exclusive, non-blocking advisory lock."}
{"id": "C035", "verdict": "SUPPORTS", "evidence_path": "src/marianne/cli/__init__.py", "evidence_excerpt": "    if value:\n        console.print(f\"Marianne AI Compose v{__version__}\")\n        raise typer.Exit()", "notes": "The function prints `__version__` imported from `marianne` when `value` is True."}
{"id": "C036", "verdict": "SUPPORTS", "evidence_path": "src/marianne/daemon/baton/musician.py", "evidence_excerpt": "    if _pricing_missing(cost_per_1k_input, cost_per_1k_output):\n...\n        return 0.0", "notes": "When pricing is missing, `_estimate_cost` returns `0.0` and `cost_uncertain` evaluates to `True`."}
{"id": "C037", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/config/job.py", "evidence_excerpt": "    state_backend: Literal[\"json\", \"sqlite\"] = Field(\n        default=\"sqlite\",\n        description=\"State storage backend\",\n    )", "notes": "`state_backend` is a Literal accepting 'json' or 'sqlite' and defaulting to 'sqlite'."}
{"id": "C038", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/config/job.py", "evidence_excerpt": "        ws_val = data.get(\"workspace\")\n        if ws_val:\n            ws = Path(str(ws_val))\n            if not ws.is_absolute():\n                data[\"workspace\"] = str((path.resolve().parent / ws).resolve())", "notes": "Pre-resolves relative workspace paths relative to the score file's parent directory before validation."}
{"id": "C039", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/config/job.py", "evidence_excerpt": "        if self.template is not None and self.template_file is not None:\n            raise ValueError(\n                \"PromptConfig accepts 'template' or 'template_file', not both\"\n            )", "notes": "Model validator raises `ValueError` if both source fields are provided."}
{"id": "C040", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/sheet.py", "evidence_excerpt": "            # Old terminology (aliases — kept forever)\n            \"stage\": self.movement,\n            \"instance\": self.voice,\n            \"fan_count\": self.voice_count,\n            \"total_stages\": total_movements,", "notes": "Provides aliases mapping 'stage', 'instance', 'fan_count', and 'total_stages' to their new movement/voice counterparts in sheet context variables."}
{"id": "C041", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/checkpoint.py", "evidence_excerpt": "class JobStatus(str, Enum):\n    \"\"\"Status of an entire job run.\"\"\"\n\n    PENDING = \"pending\"\n    RUNNING = \"running\"\n    COMPLETED = \"completed\"\n    FAILED = \"failed\"\n    PAUSED = \"paused\"\n    PAUSED_AT_CHAIN = \"paused_at_chain\"\n    CANCELLED = \"cancelled\"", "notes": "The enum defines exactly these 7 job status states."}
{"id": "C042", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/checkpoint.py", "evidence_excerpt": "    PENDING = \"pending\"\n    READY = \"ready\"\n    DISPATCHED = \"dispatched\"\n    IN_PROGRESS = \"in_progress\"\n    WAITING = \"waiting\"\n    RETRY_SCHEDULED = \"retry_scheduled\"\n    FERMATA = \"fermata\"\n    COMPLETED = \"completed\"\n    FAILED = \"failed\"\n    SKIPPED = \"skipped\"\n    CANCELLED = \"cancelled\"", "notes": "Defines 11 status types in the `SheetStatus` enum class."}
{"id": "C043", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/checkpoint.py", "evidence_excerpt": "class OutcomeCategory(str, Enum):\n    \"\"\"Classification of sheet execution outcome (#7).\"\"\"\n\n    SUCCESS_FIRST_TRY = \"success_first_try\"\n    SUCCESS_RETRY = \"success_retry\"\n    SUCCESS_COMPLETION = \"success_completion\"\n    FAILED_EXHAUSTED = \"failed_exhausted\"\n    FAILED_FATAL = \"failed_fatal\"\n    SKIPPED_BY_ESCALATION = \"skipped_by_escalation\"", "notes": "The enum defines exactly these 6 outcome classification categories."}
{"id": "C044", "verdict": "SUPPORTS", "evidence_path": "src/marianne/state/sqlite_backend.py", "evidence_excerpt": "SCHEMA_VERSION = 4", "notes": "The `SCHEMA_VERSION` constant is set to 4."}
{"id": "C045", "verdict": "SUPPORTS", "evidence_path": "src/marianne/learning/store/base.py", "evidence_excerpt": "    SCHEMA_VERSION = 15", "notes": "The `SCHEMA_VERSION` constant is set to 15."}
{"id": "C046", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/errors/codes.py", "evidence_excerpt": "class Severity(IntEnum):\n    \"\"\"Severity levels for error classification.\n\n    Lower numeric value = higher severity. This allows comparisons like\n    \\`if severity <= Severity.ERROR\\` to check for serious issues.\n...\n    CRITICAL = 1\n    ERROR = 2\n    WARNING = 3\n    INFO = 4", "notes": "Defines `Severity(IntEnum)` where `CRITICAL = 1`, `ERROR = 2`, `WARNING = 3`, `INFO = 4` (so lower values are higher severity)."}
{"id": "C047", "verdict": "CONTRADICTS", "evidence_path": "src/marianne/core/errors/codes.py", "evidence_excerpt": "        if len(self.value) >= 2:\n            first_digit = self.value[1]\n            return category_map.get(first_digit, \"unknown\")", "notes": "The categories are parsed from the first digit (which is the second character, index 1, e.g. '9' in 'E904') of the error code string, not the second digit (which would be index 2, e.g. '0' in 'E904')."}
{"id": "C048", "verdict": "SUPPORTS", "evidence_path": "src/marianne/core/checkpoint.py", "evidence_excerpt": "MAX_OUTPUT_CAPTURE_BYTES: int = 51200  # 50KB - last N bytes of stdout/stderr to capture", "notes": "The `MAX_OUTPUT_CAPTURE_BYTES` constant is set to 51200."}
